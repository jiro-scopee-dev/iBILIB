# Server port
PORT=5000

# SQLite connection string (Prisma). Change to PostgreSQL/MySQL later if needed.
DATABASE_URL="file:./dev.db"

# Directory (relative to backend/) where uploaded files are stored
UPLOAD_DIRECTORY="uploads"
